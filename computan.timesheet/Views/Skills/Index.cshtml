@model IEnumerable<computan.timesheet.core.Skill>
@{ ViewBag.Title = "List Skills"; }
<div class="panel panel-default" id="maincontent">
    @Html.Partial("_NavTabs", "skills")
    <div class="panel">
        <div class="panel-heading bg-teal-700">
            <div class="row">
                <div class="col-xs-12">
                    <div class="col-xs-12 col-sm-7">
                        <h4 class="panel-title ticket-title">
                            <span>Skills Listing </span>
                        </h4>
                    </div>
                    <div class="col-xs-12 col-sm-5">
                        <div class="heading-elements">
                            <ul class="icons-list">
                                <li>
                                    <button class="label border-left-primary label-striped" onclick="window.location.href = '/skills/create'">Add New Skill</button>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="panel panel-flat">
            <div id="DataTables_Table_0_wrapper" class="dataTables_wrapper no-footer">
                <div class="datatable-scroll">
                    <table class="table datatable-button-html5-basic" id="DataTables_Table_0" role="grid" aria-describedby="DataTables_Table_0_info">
                        <thead>
                        <tr role="row">
                            <th> @Html.DisplayNameFor(model => model.name) </th>
                            <th> @Html.DisplayName("Status") </th>
                            <th></th>
                        </tr>
                        </thead>
                        <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td> @Html.DisplayFor(modelItem => item.name) </td>
                                <td>
                                    <span class="label @(item.isactive ? "label-success" : "label-danger")">@(item.isactive ? "Active" : "In-Active")</span>
                                </td>
                                <td> @Html.ActionLink("Edit", "Edit", new {item.id}, new {@class = "btn btn-info btn-sm"}) </td>
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>